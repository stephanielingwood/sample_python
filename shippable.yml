language: python

python:
  - 2.7

addons:
  selenium: 3.1
  
#services:
#  - selenium

build:
  # pre_ci_boot:
  #   image_name: manishas/myImage
  #   image_tag: latest
  #   pull: true
  #   options: "-e HOME=/root"
  ci:
    # - node --version
    # - ls -la ~/ | grep bash
    # - echo "source ~/.nvm/nvm.sh" >> ~/.bashrc
    # - ls -la ~/ | grep nvm
    # - nvm --help
    # - npm --version
    # - npm update
    # - npm --version
    - echo "hooray selenium!"
    - java --version
    - sudo su -c "java -jar /srv/selenium-server-standalone-3.1.0.jar -Djava.security.egd=file:///dev/urandom -port 4444"

# integrations:
#   hub:
#     - integrationName: stephDocker
#       type: docker

