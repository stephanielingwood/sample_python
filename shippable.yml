language: python

python:
  - 2.7

build:
  cache: true
  ci:
    - pip install celery
    - mkdir -p shippable/testresults
    - mkdir -p shippable/codecoverage
    - cd source_apps/
    - py.test
    - cd ..
