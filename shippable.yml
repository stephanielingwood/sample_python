language: python

python:
- 2.7

install:
  - pip install celery

before_script: 
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage

script:
  - cd source_apps/
  - py.test
  - cd ..

cache: true
