language: python

python:
- 2.7

install:
  - pip install celery
  - which celery
  - sudo apt-get install tree
  - tree

before_script: 
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage

script:
  - cd source_apps/
  - py.test
  - cd ..

cache: true
