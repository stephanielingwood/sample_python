language: python

python:
  - 2.7

#addons:
#  - selenium: 3.1
  
services:
  - selenium

build:
  # pre_ci_boot:
  #   image_name: manishas/myImage
  #   image_tag: latest
  #   pull: true
  #   options: "-e HOME=/root"
  ci:
    # - node --version
    # - ls -la ~/ | grep bash
    # - echo "source ~/.nvm/nvm.sh" >> ~/.bashrc
    # - ls -la ~/ | grep nvm
    # - nvm --help
    # - npm --version
    # - npm update
    # - npm --version
    - selenium --version

# integrations:
#   hub:
#     - integrationName: stephDocker
#       type: docker

