language: python

python:
  - 3.5

env:
  global:
    - TEST_VAR=reallyAwesomeString

build:
  # pre_ci_boot:
  #   image_name: manishas/myImage
  #   image_tag: latest
  #   pull: true
  #   options: "-e HOME=/root"
  ci:
    - pwd
    - echo $JOB_STATE
    - tar -C $SHIPPABLE_BUILD_DIR -xcvf $JOB_STATE/archive.tar.gz .
